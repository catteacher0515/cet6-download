{
  "name": "cet6-download",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "tsx --test tests/**/*.test.ts",
    "validate:content": "tsx scripts/validate-content.ts",
    "prepare:paper": "tsx scripts/prepare-paper.ts",
    "prepare:session": "tsx scripts/prepare-session.ts",
    "prebuild": "npm run validate:content",
    "check": "astro check && npm run test"
  },
  "dependencies": {
    "astro": "^6.3.1",
    "jszip": "^3.10.1",
    "pdfjs-dist": "^5.7.284"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
